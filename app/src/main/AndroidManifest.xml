<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.t5">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <dist:module dist:instant="true" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="Go Goa"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewPostActivity"></activity>
        <activity android:name=".NewPostActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Vehicle" />
        <activity android:name=".HospitalAdmin" />
        <activity android:name=".VehicleAdmin" />
        <activity android:name=".ChurchAdmin" />
        <activity android:name=".MarketAdmin" />
        <activity android:name=".FortAdmin" />
        <activity android:name=".TempleAdmin" />
        <activity android:name=".HotelAdmin" />
        <activity android:name=".BeachAdmin" />
        <activity android:name=".DashboardAdmin" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".UpdateProfile" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Information" />
        <activity android:name=".Church" />
        <activity android:name=".Fort" />
        <activity android:name=".Dashboard" />
        <activity android:name=".ProfessionalAccount" />
        <activity android:name=".Colleges" />
        <activity android:name=".Market" />
        <activity android:name=".Temple" />
        <activity android:name=".Hotel" />
        <activity android:name=".Hospital" />
        <activity android:name=".Beach" />
        <activity android:name=".Rate" />
        <activity android:name=".Emergency" />
        <activity android:name=".Contact" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Main4Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main3Activity" />
        <activity android:name=".Profile" />
        <activity android:name=".Post" />
        <activity android:name=".Search" />
    </application>

</manifest>